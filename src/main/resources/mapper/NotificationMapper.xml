<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leave.mapper.NotificationMapper">

    <insert id="insert" parameterType="com.leave.entity.Notification">
        INSERT INTO notification (user_id, title, content)
        VALUES (#{userId}, #{title}, #{content})
    </insert>

    <select id="findByUserId" resultType="com.leave.entity.Notification">
        SELECT * FROM notification
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <update id="markRead">
        UPDATE notification SET is_read = 1 WHERE id = #{id}
    </update>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM notification WHERE user_id = #{userId} AND is_read = 0
    </select>

</mapper>
